# Server Configuration
PORT=8080
HOST=0.0.0.0

# Database Configuration (PostgreSQL)
# Connection string format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://nostr:password@localhost:5432/nostr_relay

# For Cloud SQL via Unix socket:
# DATABASE_URL=postgresql://nostr:password@localhost/nostr_relay?host=/cloudsql/PROJECT:REGION:INSTANCE

# For Docker Compose:
# DATABASE_URL=postgresql://nostr:password@postgres:5432/nostr_relay

# Whitelist Configuration
# Comma-separated list of allowed Nostr pubkeys (hex format)
# Leave empty for development mode (allows all pubkeys)
WHITELIST_PUBKEYS=

# Admin Configuration
# Comma-separated list of admin pubkeys (hex format)
# Admins can approve users and manage the whitelist
ADMIN_PUBKEYS=

# Example with pubkeys:
# WHITELIST_PUBKEYS=abc123...,def456...,ghi789...
# ADMIN_PUBKEYS=abc123...

# NIP-11 Relay Information
# These values are served in the relay information document
# See: https://github.com/nostr-protocol/nips/blob/master/11.md
RELAY_NAME=Fairfield Nostr Relay
RELAY_DESCRIPTION=Private whitelist-only relay for the Fairfield BBS community
RELAY_CONTACT=nostr:your_admin_pubkey_here
RELAY_BASE_URL=https://fairfield.dev

# Rate Limiting Configuration
# Maximum events per second per IP address
RATE_LIMIT_EVENTS_PER_SECOND=10

# Maximum concurrent connections per IP address
RATE_LIMIT_MAX_CONNECTIONS=20

# Cleanup interval for expired rate limit entries (milliseconds)
RATE_LIMIT_CLEANUP_INTERVAL_MS=30000
